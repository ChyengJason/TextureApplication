<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jscheng.textureapplication">
    <uses-permission android:name="android.permission.RECORD_AUDIO"/>
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.FliterActivity"/>
        <activity android:name=".activity.ImageActivity"/>
        <activity android:name=".activity.CameraActivity"/>
        <activity android:name=".activity.ImageViewActivity"/>
        <activity android:name=".activity.ImageSurfaceActivity"/>
        <activity android:name=".activity.CustomViewActivity"/>
        <activity android:name=".activity.RecordingActivity"/>
        <activity android:name=".activity.CameraPreviewActivity"/>
        <activity android:name=".activity.SurfaceviewPreviewActivity"/>
        <activity android:name=".activity.TextureviewPreviewActivity"/>
    </application>

</manifest>